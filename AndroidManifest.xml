<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.nologin.meep.pingly"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <application
        android:icon="@drawable/smile"
        android:label="@string/app_name" 
        android:theme="@style/Theme.D1">
        <activity
            android:label="@string/app_name"
            android:name=".activity.PinglyDashActivity">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TaskListActivity" android:label="@string/app_name"></activity>
        <!--  android:windowSoftInputMode="adjustPan" stops the buttons being moved when the scrollview is resized for soft keyboard  -->
        <activity android:name=".activity.TaskDetailsActivity" android:label="@string/app_name" android:windowSoftInputMode="adjustPan"></activity>
        <activity android:name=".activity.TaskRunnerActivity" android:label="@string/app_name"></activity>
    </application>

</manifest>